# OS/Distro-Specific Shell Scripts

## Supported Distros
- Debian/Ubuntu (apt)
- RHEL/Fedora (dnf/yum)
- Arch (pacman)

## Package Management Examples
Use `if command -v apt` checks.

See `docker-monitor.sh` for systemd examples.

**Prompt tip:** Specify target distro in LLM prompts.